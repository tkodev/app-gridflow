# Solution

**Partially TBD.** No solution-stage decisions (concept rationale, chosen systems) have been
documented yet. The route table below reflects what's actually implemented in `app/` today —
observed fact, not a settled spec — until a real solution stage is written.

## Surfaces (as implemented)

| Route | Purpose |
| --- | --- |
| `/` | Home |
| `/plan` | The grid-preview planning surface — arranges posts into a true-to-life feed grid |
| `/collect` | Stub only (placeholder page, not yet built) |
| `/settings` | Profile settings |
| `/auth/sign-in`, `/auth/sign-up`, `/auth/sign-out`, `/auth/sign-up-success`, `/auth/error` | Supabase auth flow |

## Systems

TBD — no shared UI systems/patterns have been documented at concept level yet.
