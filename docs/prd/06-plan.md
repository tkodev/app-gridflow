# Plan

**TBD.** No milestone graph has been documented yet. What's known from `01-brief.md`:

- **MVP** — the mockup/planning tool (grid preview, arranging posts). `/plan` and
  `/settings` are implemented; `/collect` exists only as a stub.
- **Stretch, post-MVP** — direct Instagram connection and scheduling (cron infrastructure
  reserved for this phase).

Cut a real milestone graph (system → surface → sweep, per the `process` skill) once the
observations and solution stages above are filled in.
